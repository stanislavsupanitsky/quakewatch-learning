apiVersion: v1
kind: Service
metadata:
  name: quakewatch-learning
spec:
  type: NodePort
  selector:
    app: quakewatch-learning
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
